name: Git restore the state of a file for a known good hash
command: git restore --source={{hash}} {{file_path}}
arguments:
  - name: hash
    description: the short commit hash
  - name: file_path
    description: (str) the path to the file to reset

tags: ["git"]
